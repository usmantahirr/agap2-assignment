import { ReactNode } from "react"
import { Container } from "@mui/material"

const Template = ({ children }: { children: ReactNode }): ReactNode => <Container>{children}</Container>

export default Template

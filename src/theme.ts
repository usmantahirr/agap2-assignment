import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    primary: {
      main: "#34495e",
    },
    secondary: {
      main: "#9b59b6",
    },
    error: {
      main: "#e74c3c",
    },
  },
})

export default theme
